org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# This modern build uses Android Gradle Plugin 9, which requires running Gradle with JDK 17.
# Java 25 can be installed on your machine, but the Android build should be run with JAVA_HOME pointing to JDK 17.

